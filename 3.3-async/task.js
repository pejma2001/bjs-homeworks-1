class alarmClock {
	constructor{
		constructor(){
		alarmCollection() = [];
		timerId(id);
	}
	addClock(){
		setTimeout(){
			let date = new Date;
			const start = date.getHours() + "." + date.getMinutes();
		}
	}
	}
}